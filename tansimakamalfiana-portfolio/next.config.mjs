/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.freepik.com',
                  'images.unsplash.com',
                  'i.postimg.cc',
                ],
    },
};

export default nextConfig;
